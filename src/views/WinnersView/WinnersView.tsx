import React from "react";
import { FC } from "react";

interface WinnersViewProps {

}

export const WinnersView: FC<WinnersViewProps> = React.memo(function WinnersView(props: WinnersViewProps) {
  const { } = props;

  return <>{}</>;
});
